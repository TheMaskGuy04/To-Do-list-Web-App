<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List Web App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="styles/style.css">
</head>
<body>
    <div class="container">
        <div class="add">
            <!-- <a href="/new_note">Add a new Note</a> -->
            <form action="/new_note" method="GET">
                <button type="submit" class="btn btn-primary">Add a New Note</button>
           </form>
        </div>
    
        <div class="all-notes">
            <h2>Your Notes</h2>
            <% if(locals.title && locals.note) { %>
                <% for( let i = 0; i < title.length; i++ ) { %>
                    <div id="<%= i %>" class="notes">
                        <h3><%= title[i] %></h3>
                        <p><%= note[i] %></p>

                        <div class="delete-update">
                            <form action="/update" method="POST">
                                 <button type="submit" value="<%= i %>" name="update" class="btn btn-primary">Update</button>
                            </form>
                           <form action="/delete" method="POST">
                                <button type="submit" value="<%= i %>" name="delete" class="btn btn-primary">Delete</button>
                           </form>
                        </div>
                    </div>
                <% } %>
            <% } else { %>
                <%- "No Notes Created" %>
            <% } %>
        </div>
    </div>
</body>
</html>